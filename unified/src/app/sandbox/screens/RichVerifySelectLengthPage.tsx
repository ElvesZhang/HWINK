interface Props { showDebugId?: boolean }

// Placeholder for stage A. Real layout lands in stage F (with RecoveryHero
// illustration).
export function RichVerifySelectLengthPage(_: Props) {
  return (
    <div className="flex-1 flex flex-col items-center justify-center text-center px-6">
      <div className="text-sm font-bold text-black uppercase tracking-widest mb-2">Rich Verify · Select Length</div>
      <p className="text-xs text-black leading-snug max-w-[240px]">
        Stage A placeholder. RecoveryHero illustration lands in stage F.
      </p>
    </div>
  );
}
